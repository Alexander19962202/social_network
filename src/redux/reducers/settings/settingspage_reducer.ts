let initialState = {}

const settingsPage_reducer = (state = initialState, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
}

export default settingsPage_reducer;
